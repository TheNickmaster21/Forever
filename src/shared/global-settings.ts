export const GlobalSettings = {
    voxelSize: 4,
    gridHeight: 8,

    chunkSize: 16,

    shownRadius: 6,

    garbageTriggerChunkCount: math.pow(6 * 3, 3),
    garbageCollectionIncrement: 100
};
