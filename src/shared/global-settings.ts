export const GlobalSettings = {
    voxelSize: 4,
    gridHeight: 8,

    chunkSize: 16,

    shownRadius: 6,

    craterFrequency: 0.3,
    caveFrequency: 0.5,

    garbageTriggerChunkCount: 400,
    garbageCollectionIncrement: 10
};
